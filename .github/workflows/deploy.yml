name: Deploy Presentismo

on:
push:
branches:
- main

jobs:
build:
runs-on: windows-latest

```
steps:
  - name: Checkout
    uses: actions/checkout@v4

  - name: Configurar MSBuild
    uses: microsoft/setup-msbuild@v2

  - name: Verificar MSBuild
    run: msbuild -version

  - name: Verificar archivos
    run: dir
```
